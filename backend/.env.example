NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://user:password@localhost:5432/dayflow_dev
DATABASE_URL_TEST=postgresql://user:password@localhost:5432/dayflow_test

# ADDED IN PHASE 02: auth-secret / JWT-secret / session-secret env vars.
# Deliberately not present in Phase 01 — do not add them until Phase 02 defines them.
