# Modules (Phase 01 placeholder)

Business logic will live in one folder per domain module. Planned folders per the
master roadmap — do not create them until their phase begins:

- `auth/` — Phase 02: sign up/sign in, login ID generation, password hashing,
  account provisioning.
- `employees/` — Phase 04 (+ Phase 08 documents): employee profile management.
- `attendance/` — Phase 06.
- `leave/` — Phase 07.
- `payroll/` — Phase 09.

Nothing else belongs here yet.
