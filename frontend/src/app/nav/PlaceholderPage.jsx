// Attendance (Phase 06) and Time Off (Phase 07) nav tabs route here — no functional content,
// per the Phase 05 hard-stop conditions.
export function PlaceholderPage({ title }) {
  return (
    <div className="placeholder-page">
      <p className="tab-stub">{title} — coming soon</p>
    </div>
  );
}
