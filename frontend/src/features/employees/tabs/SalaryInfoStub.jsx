// Phase 08 builds real content — this phase renders only the stub the design's tab layout
// requires. No salary fields, endpoints, or calculation logic belong here.
export function SalaryInfoStub() {
  return <p className="tab-stub">Salary information — coming soon</p>;
}
