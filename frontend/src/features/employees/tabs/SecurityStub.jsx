// TODO(D-10): neither source document specifies this tab's contents; the design shows it only
// on the Employee's own profile (see ProfilePage.jsx's tab-set logic). Rendered as a
// visible-but-empty stub only — no real content belongs here yet.
export function SecurityStub() {
  return <p className="tab-stub">Security — coming soon</p>;
}
